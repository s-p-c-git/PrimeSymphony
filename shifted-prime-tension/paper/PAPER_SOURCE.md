# Paper Source

The authoritative, compile-ready source for the paper is **`main.tex`**
(builds to ~8 pages with `pdflatex`; figures in `figures/`).

An earlier Markdown draft previously lived here; it has been retired to avoid
drift. See `main.tex` for the final title, abstract, theorems, and proofs, and
`AUTHOR_IDENTITY.md` for author/submission metadata.
